enum DataMode { auto, real, mock }

enum BookingStatus { confirmed, pending, cancelled, completed, refundPending, refunded }

enum PaymentStatus { success, failure, pending, cancelled }

enum PropertyType { hotel, resort, villa, apartment, cottage }

enum AmenityType { wifi, pool, gym, restaurant, spa, parking, ac, breakfast }
